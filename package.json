{
  "name": "aws-lambda",
  "version": "0.1.0",
  "type": "module",
  "bin": {
    "aws-lambda": "bin/aws-lambda.js"
  },
  "scripts": {
    "build": "echo \"The build step is not required when using JavaScript!\" && exit 0",
    "cdk": "cdk",
    "test": "jest"
  },
  "devDependencies": {
    "aws-cdk": "2.82.0",
    "jest": "^29.5.0"
  },
  "dependencies": {
    "aws-cdk-lib": "2.82.0",
    "constructs": "^10.0.0"
  }
}
